# Neural Guard - Frontend Environment Configuration

# API Base URL (optional - defaults to /api if not set)
VITE_API_BASE_URL=http://localhost:8000

# Refresh interval in milliseconds
VITE_REFRESH_INTERVAL=5000

name: Run ARM64 first SITL pair

on:
  workflow_dispatch:
    inputs:
      scenario:
        description: Scenario from the approved catalog
        required: true
        default: motor_imbalance
        type: string
      frame:
        description: ArduPilot frame
        required: true
        default: quad
        type: choice
        options: [quad, hexa, octa]
      seed:
        description: Deterministic planning seed
        required: true
        default: '20260840'
        type: string
      randomize:
        description: Draw capability-checked noise/vibration/battery parameters
        required: false
        default: 'off'
        type: choice
        options: [off, on]

permissions:
  contents: read
  packages: read

env:
  SITL_IMAGE: ghcr.io/beastayyg/ardupilot-log-diagnosis@sha256:fcea4016c58485695bc12afc119e3b6dc4e44c525d813ceab7792ee69ce2fb9b

jobs:
  first-pair:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 45
    steps:
      - name: Check out launcher
        uses: actions/checkout@v4

      - name: Authenticate to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run one verified pair
        env:
          PAIR_OUTPUT_DIR: ${{ github.workspace }}/artifacts/first-pair
          PAIR_SCENARIO: ${{ inputs.scenario }}
          PAIR_FRAME: ${{ inputs.frame }}
          PAIR_SEED: ${{ inputs.seed }}
          PAIR_RANDOMIZE: ${{ inputs.randomize }}
        run: bash ops/dgx/run_first_pair.sh

      - name: Upload first-pair evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arm64-first-pair-evidence
          path: artifacts/first-pair
          if-no-files-found: error
          retention-days: 14
